import {} from "../constants/actions";

// source de vérité
const stateInit = {
  choices : []
};

// algo
const reducer = (state = stateInit, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;